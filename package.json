{
  "name": "tuialailima-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:index": "node scripts/build-index.mjs",
    "stamp": "node scripts/stamp-metadata.mjs --apply && node scripts/build-index.mjs"
  }
}
